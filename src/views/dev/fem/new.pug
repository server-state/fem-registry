extends ../layout

block append breadcrumbs
    li
        span New FEM

block innercontent
    p Please insert the FEMs metadata. This can be changed later.
    if error
        .uk-alert-danger(uk-alert)=error
    form(method='post')
        input(type='hidden' name='_csrf' value=csrfToken)
        h3 Required Fields
        label.uk-form-label Project Name (for internal use, not visible to users)
            input.uk-input(name='name' required)
        br
        input.uk-button.uk-button-primary.uk-margin(type='submit' value='Create FEM')
