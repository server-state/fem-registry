extends ../../layout

block content
    h1 Change name
    if error
        p=error
    form(method='post')
        p
            label Name
                input(type='text' name='name' value=user.name required)
        input(type='submit' value='Change password')

